import { color } from './color';
import { font, fontSize, fontWeight } from './font';
import { lineHeight, space } from './spacing';
import { size } from './size';

export const theme = {
  color,
  font,
  fontSize,
  fontWeight,
  lineHeight,
  space,
  size,
} as const;
